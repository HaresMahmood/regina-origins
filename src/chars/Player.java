package chars;

public class Player extends Character {
    // Constructor
    public Player(Position position) {
        super(position);
        this.name = "Donald";
        this.symbol = 'P';
    }
}